security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    # Users live in the database and log in with their username.
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: username

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            # No session and no login form: the firewall is stateless. POST
            # /auth/login exchanges credentials for a JWT via json_login; every
            # other request is authenticated by the JWT in its Authorization
            # header. Both authenticators return a JSON 401 on failure.
            stateless: true
            provider: app_user_provider
            json_login:
                check_path: auth_login
                username_path: username
                password_path: password
                success_handler: lexik_jwt_authentication.handler.authentication_success
                failure_handler: lexik_jwt_authentication.handler.authentication_failure
            jwt: ~

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used, so the two
    # public routes are listed before the catch-all.
    access_control:
        # The /health probe must stay public — it is hit by an external uptime
        # monitor and must not sit behind any edge authentication.
        - { path: ^/health$, roles: PUBLIC_ACCESS }
        # Login exchanges credentials for a token, so it cannot require one.
        - { path: ^/auth/login$, roles: PUBLIC_ACCESS }
        # Everything else — the API and its Scalar docs — needs a valid token.
        - { path: ^/, roles: IS_AUTHENTICATED_FULLY }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
